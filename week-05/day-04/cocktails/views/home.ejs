<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <title>
        <%= title %>
    </title>
    <link rel="stylesheet" type="text/css" href="static/style.css">
</head>

<body>
    <h1>
        <%= title %>
    </h1>
    <div>
        <a href="/">All</a>
        <% alcoholList.forEach( alcohol =>{%>
            <a href="<%= " /?alcohol="+alcohol %>">
                <%= alcohol %>
            </a>
            <%})%>
    </div>
    <table>
        <thead>
            <tr>
                <th>NAME</th>
                <th>PRICE</th>
            </tr>
        </thead>
        <tbody>
            <% cocktails.forEach( cocktail => { %>
                <tr>
                    <td>
                        <h5>
                            <%= cocktail.name %> <span><%= cocktail.isAlcoholic ? "" : "(no-Alcoholic)" %></span></h5>
                        <p>
                            <%= cocktail.contains %>
                        </p>
                    </td>
                    <td className="price">
                        <%= cocktail.price %> Ft</td>
                </tr>
                <% }) %>

        </tbody>
    </table>
</body>

</html>